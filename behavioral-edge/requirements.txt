# Core
pandas>=2.0.0
numpy>=1.24.0
pyyaml>=6.0
python-dotenv>=1.0.0

# Database
# sqlite3 is built-in; install aiosqlite for async access if needed
aiosqlite>=0.19.0

# HTTP / API
requests>=2.31.0
tenacity>=8.2.0          # retry logic
httpx>=0.24.0            # async HTTP (optional)

# Polygon.io
polygon-api-client>=1.12.0

# Reddit / social
praw>=7.7.0
apewisdom-api>=0.1.0     # unofficial; we call the REST endpoint directly

# Sentiment
vaderSentiment>=3.3.2
# FinBERT (heavier, optional — enable in settings.yaml)
# transformers>=4.35.0
# torch>=2.0.0

# Earnings / catalyst data
yfinance>=0.2.36
finnhub-python>=2.4.19

# Analysis / backtest
scipy>=1.11.0
scikit-learn>=1.3.0

# Logging
structlog>=23.1.0
rich>=13.5.0              # pretty console output

# CLI
click>=8.1.0

# ML (Phase 2 — install separately when ready)
# torch>=2.0.0
# stable-baselines3>=2.1.0

# Notebooks
jupyterlab>=4.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.17.0
